# hexa.toml — hexa-bio standalone package manifest
#
# Spec: shared/hexa-lang/hexa-toml-spec.json (version 1.0.0)
# Distribution target: hx install hexa-bio
# Provenance: extracted from nexus/sim_bridge/weave/ on 2026-05-04
#             (canonical-from cycle 24, 2026-04-29 — canon sister)

[package]
name = "hexa-bio"
version = "1.0.0"
entry = "cli/hexa-bio.hexa"
description = "Molecular Toolkit (HEXA family): 5-axis — QUANTUM/WEAVE/NANOBOT/RIBOZYME/VIROCAPSID — organized around the n=6 invariant lattice (σ(6)=12, τ(6)=4, φ(6)=2, J₂=24). The 4 bio axes form the τ-quartet tetrahedron (weave wired with cage-assembly ODE + Caspar-Klug Bayesian audit; nanobot/ribozyme/virocapsid ship C0b skeleton simulators + falsifier preregister); the 5th axis (quantum) is the qpu_bridge VQE compute layer over qmirror (Phase 1+: H₂/LiH chemical/spectroscopic accuracy, F-Q-1…5 PASS, pocket-VQE F-Q-6 open). 5-axis count is locked. raw#10 honest C3."
license = "Apache-2.0"
authors = ["박민우 <nerve011235@gmail.com>"]
repository = "https://github.com/dancinlab/hexa-bio"
homepage = "https://github.com/dancinlab/hexa-bio"
readme = "README.md"
keywords = ["molecular-toolkit", "hexa-family", "quantum", "weave", "nanobot", "ribozyme", "virocapsid", "caspar-klug", "n6-invariant", "vqe", "qpu-bridge"]
categories = ["bioinformatics", "molecular-simulation", "structural-biology", "science"]

[lib]
# Library entry — importable by other hexa packages.
entry = "weave/module/weave.hexa"

[[bin]]
name = "hexa-bio"
path = "cli/hexa-bio.hexa"

[test]
runner = "hexa"
files = [
    "tests/test_weave.hexa",
    "tests/test_nanobot.hexa",
    "tests/test_ribozyme.hexa",
    "tests/test_virocapsid.hexa",
    "tests/test_quantum.hexa",
    "tests/test_quantum_pilot_runner.hexa",
    "tests/test_selftest.hexa",
]

# ── hx install hook ───────────────────────────────────────
# install.hexa runs under hx package manager (hexa-lang) at install time.
# Phases: pre   → optional python_bridge deps (numpy/scipy for weave only;
#                 OPT-IN via HEXA_BIO_WITH_NUMPY=1, default skipped).
#         post  → quick selftest (warn-only, not a blocker).
[scripts]
install = "install.hexa"

# ── Modules manifest (5 axes + selftest) ──────────────────
# 5-axis framework (locked — .roadmap.axis_expansion_decision_2026_05_08):
#   weave      — IMPORTED from nexus/sim_bridge/weave/ (cycle 24 canonical) — WIRED
#   nanobot    — C0b skeleton (DNA-origami actuation sim + falsifier preregister)
#   ribozyme   — C0b skeleton (hammerhead-minimal kinetics sim + falsifier preregister)
#   virocapsid — C0b skeleton (capsid assembly sim + RCSB PDB corpus + falsifier preregister)
#   quantum    — qpu_bridge VQE / ML pilot dispatcher (Phase 1+; Python adapters in _python_bridge)
[modules]
hexa = [
    "weave/module/weave.hexa",
    "nanobot/module/nanobot.hexa",
    "ribozyme/module/ribozyme.hexa",
    "virocapsid/module/virocapsid.hexa",
    "quantum/module/quantum.hexa",
    "quantum/module/external_pilot_runner.hexa",
    "quantum/module/n6_lattice_check.hexa",
    "selftest/module/selftest.hexa",
]
python_bridge_aux = [
    # weave-only (raw#9 .own N opt-out — numpy/scipy heavy lifting).
    # Loaded only when HEXA_BIO_WITH_NUMPY=1; pure-hexa path is default.
    "_python_bridge/module/cage_assembly_simulation.py",
    "_python_bridge/module/polyhedral_cage_bayesian_audit.py",
    # virocapsid PDB corpus fetch (stdlib urllib only — no opt-in needed).
    "_python_bridge/module/virocapsid_pdb_corpus.py",
    # quantum axis: VQE adapters bridging the qmirror CLI (qiskit-aer opt-in;
    # see .roadmap.quantum). Default `hexa-bio quantum` needs no extra deps.
    # (quantum_*.py adapters loaded only on the full VQE path.)
]

# ── Dependencies ───────────────────────────────────────────
# hexa stdlib: required for all 5 axes (CLI dispatch, falsifier table, status).
# Optional Python deps:
#   numpy >= 1.24      — array ops for weave ODE state vec       (BSD-3, compatible)
#   scipy >= 1.11      — weave Bayesian audit pipeline           (BSD-3, compatible)
#   qiskit-aer >= 0.13 — quantum axis full VQE path (opt-in)     (Apache-2.0, compatible)
#
# Default paths (4-bio-axis skeletons + CLI router + status + `quantum` snapshot)
# require ZERO Python — pure hexa-lang.

[dependencies]
hexa-lang = ">=1.0.0"

[dependencies.optional]
python_aux_bsd3 = "numpy >= 1.24, scipy >= 1.11 (weave empirical path only; BSD-3 in-process safe; opt-in via HEXA_BIO_WITH_NUMPY=1)"
python_aux_quantum = "qiskit-aer >= 0.13 + qmirror (install via `hx install qmirror`) — quantum axis full VQE path only; Apache-2.0 in-process safe; default `hexa-bio quantum` prints a status snapshot with zero extra deps"

# ── Closure provenance (raw#10 honest C3) ─────────────────
# v1.0.0 closure: 1/5 axes fully wired (weave) + 3/5 C0b skeleton
# (nanobot/ribozyme/virocapsid) + 1/5 Phase-1+ (quantum: H₂/LiH VQE,
# F-Q-1…5 PASS, pocket-VQE F-Q-6 open).
# Honest scope: weave is empirical-sandbox only — theoretical / formal-mech
# content lives in canon/lean4-n6/N6/MechVerif/. n=6 invariant itself is
# verified via sympy + literature corpus only (no proprietary calibration).
[closure]
axes_total = 5
axes_wired = 1                    # weave
axes_c0b_skeleton = 3             # nanobot, ribozyme, virocapsid (all with deductive σ/τ/φ/J₂ PASS via n6_axis_computational_verification)
axes_phase1_plus = 1              # quantum (qpu_bridge L1)
verdict = "PARTIAL_PASS"
axis_count_lock = ".roadmap.axis_expansion_decision_2026_05_08 (5-axis; 6th/7th candidates reject/defer)"
extracted_from = "nexus/sim_bridge/weave/ @ 2026-05-04"
upstream_marker = "anima/state/markers/hexa_bio_standalone_extraction_landed.marker"
in_repo_closure_complete = true   # 2026-05-12 — σ/τ/φ/J₂ deductive (42/42) + 5 per-axis in-repo closure components landed & run_all.sh-wired; everything code/doc-closable in this repo is closed.
in_repo_closure_components = [
    "selftest/n6_axis_computational_verification.py (σ/τ/φ/J₂ + master identity, 42/42)",
    "_python_bridge/module/ribozyme_mfe_nussinov.py (ribozyme R-R1 — Nussinov O(n^3) MFE port)",
    "_python_bridge/module/ribozyme_off_target_screen.py (ribozyme G26-RB-3 comp 3 — Hamming off-target screen + representative pool)",
    "_python_bridge/module/ribozyme_reaction_coordinate_quotient.py (ribozyme G26-RB-2 — J2=|S4|=24, S4 iso O)",
    "_python_bridge/module/ribozyme_kinetics_simulation.py (ribozyme G26-RB-1prime sim re-run — Eyring TST + 4-state RK4/Euler ODE, F-RB-4 6/6 PASS; re-impl of R5-sunset original)",
    "_python_bridge/module/nanobot_actuation_simulation.py (nanobot C0d dual-skeleton sim re-run — 4-state actuation + J2=24 pose-canon, both {truncated_icosahedron, cuboctahedron} F-NB-4 6/6 PASS; re-impl of R5-sunset original)",
    "tests/mpro_pocket_vqe_v7.py (quantum F-Q-6/L3 — Mpro pocket-cluster VQE, 2e/2o sub-uHa vs CASCI; needs ~/.hexabio_venv qiskit/pyscf)",
    "selftest/virocapsid_c5_conformance.py + virocapsid/spec/cage_output_v1.schema.json lock_metadata + 4 fixtures (virocapsid GATE-26-V-R1 / C5)",
    "_python_bridge/module/nanobot_actuator_v2_reference_emit.py (nanobot N-R1 — raw_77_nanobot_actuation_v2 reference emitter)",
]
out_of_repo_residual = "quantum F-Q-6 (SARS-CoV-2 Mpro / nirmatrelvir pocket VQE — target confirmed, dedicated loop) | virocapsid C3b (n>=100 RCSB PDB corpus + Bayesian re-audit >= 0.95) | nanobot C0d (cuboctahedron dual-skeleton sim re-run) + N-R2 (canon-side L6 acceptance lock) | ribozyme G26-RB-1prime (rubric sim re-run; values already in MVP) | GATE-26-2 (lean4-backed Pi-1-1-CA0 cert, all 5 axes -> v2.0.0)"

# ── Scope honesty (raw#10 C3) ─────────────────────────────
[scope]
honest_scope = "WEAVE: empirical sandbox (cage-assembly ODE + Caspar-Klug Bayesian audit). NANOBOT/RIBOZYME/VIROCAPSID: C0b skeleton simulators + falsifier preregister. QUANTUM: qpu_bridge VQE (H₂ 0.4 µHa, LiH 1.41 mHa) + ML pilot smokes; pocket-VQE F-Q-6 open."
not_scope = ["AlphaFold-3 fold prediction (read-side; this toolkit is write-side)", "molecular dynamics (no GROMACS/NAMD bridge)", "drug discovery docking", "in-vivo validation (purely numerical)", "fault-tolerant quantum hardware (Aer simulator + ANU QRNG only)"]
n6_lattice_caveat = "n=6 invariant (σ(6)=12, τ(6)=4, φ(6)=2, J₂=24): empirically/structurally grounded for weave (vertex count Bayesian audit, posterior 0.97) and quantum (H₂ 6-Pauli × 2-qubit). nanobot 12-vertex polyhedron, ribozyme 12-nt core, and T>1 virocapsid carry the lattice claim as STRUCTURAL-EXACT-CANDIDATE pending independent verification."

# ── Cost profile ──────────────────────────────────────────
[cost]
mac_local_default = "$0 (pure hexa skeletons + python stdlib for weave; `quantum` snapshot is pure hexa)"
weave_full_audit = "$0 (textbook corpus only, no network)"
weave_extended = "$0 (CPU-only ODE; --t-end 100000 ≈ 30s wall-clock)"
nanobot_ribozyme_virocapsid = "$0 (C0b skeleton sims print falsifier table + sentinel)"
quantum_status_snapshot = "$0 (pure hexa dispatcher); full VQE path = local Aer simulator (CPU) + ANU QRNG (free public API)"
